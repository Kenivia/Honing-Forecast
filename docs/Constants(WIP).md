# Constants

The constants (honing and event parameters) are read from a json file, which is generated from a google sheet via a simple script.

The system is already in-place but the documentation isn't yet.
