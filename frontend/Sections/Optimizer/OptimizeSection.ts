import { InputsBundleWithSetters } from "@/Utils/InputBundles.ts"
import React from "react"
type OptimizeSectionProps = {
    inputs: InputsBundleWithSetters
}

export default function OptimizeSection({ inputs }: OptimizeSectionProps) {
return (
    <div style={{ ...styles.inputSection, flexDirection: "row", maxWidth: "1200px", width: "100%" }}>
    </div>
)}