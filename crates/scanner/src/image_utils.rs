use crate::scanner_state::{ImageHash, PixelPosition, ScannerState};

impl ScannerState {
    // pub fn template_match(&mut self, position: PixelPosition)  -> (PixelPosition, ImageHash) {}

    // pub fn image_hash(&mut self, position: PixelPosition) -> ImageHash {

    // }
}
