use crate::scanner_state::{ImageHash, ScaledPosition, SlotAddress};

pub const ALL_ANCHOR_TEMPLATES: [[[u8; 0]; 0]; 0] = []; // ALL_ANCHOR_TEMPLATES[InventoryType][variant] = [u8]
pub const ANCHOR_BOUNDS: [ScaledPosition; 0] = [];

pub const ALL_SLOT_ADDRESSS: [SlotAddress; 0] = [];
pub const ALL_ICONS: [(String, ImageHash); 0] = []; // pre sure string doesnt actually work? idk
