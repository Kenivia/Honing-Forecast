// import React, { useState } from "react";
// import "./CheckboxRow.css"; // CSS shown below

// export default function CheckboxRow(grid, gridSetter) {
//     // State: an array of booleans for checked state
//     const [checked, setChecked] = useState(() => grid[0].map(() => false));

//     const toggle = (index) => {
//         gridSetter((prev) => {
//             const copy = [...prev];
//             copy[index] = !copy[index];
//             return copy;
//         });
//     };

//     return (
        
//     );
// }
